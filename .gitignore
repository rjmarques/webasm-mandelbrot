# Build output (rebuilt in CI on every deploy)
dist
dist.tar
mandel.tar

# Emscripten build artifacts (generated by `make`)
mandelbrot.js
mandelbrot.wasm

# Node
node_modules

# OS / editor cruft
.DS_Store
.idea
Thumbs.db
